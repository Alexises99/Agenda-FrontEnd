import React, {useState} from 'react';

export const Form = (props) => {

    return (
        <form>
            <div>
            name: <input />
            </div>
            <div>
            <button type="submit">add</button>
            </div>
        </form>
    )
}